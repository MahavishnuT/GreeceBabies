services:
  - type: web
    name: greecebabies
    runtime: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: node index.js
    envVars:
      - key: MONGODB_URI
        sync: false
